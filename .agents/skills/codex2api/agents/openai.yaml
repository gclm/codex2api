interface:
  display_name: "Codex2api"
  short_description: "Repo-specific patterns and workflows for codex2api"
  default_prompt: "Use the codex2api repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true